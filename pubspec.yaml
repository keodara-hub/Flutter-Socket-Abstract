name: socket_abstract
description: A new Flutter package project.
version: 0.0.2
homepage: "https://ecoinsoft.com"

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.23.1
  socket_io_client: ^0.9.8

dev_dependencies:
  flutter_test:
    sdk: flutter
